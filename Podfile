project 'FamilyPlanner/FamilyPlanner.xcodeproj'

# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

target 'AppData' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for AppData
  pod 'SwiftDate', '~> 4.4.0'
  pod 'FLAnimatedImage', '~> 1.0'
end

target 'AppUI' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for AppUI

end

target 'FamilyPlanner' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for FamilyPlanner
  pod 'SwiftDate', '~> 4.4.0'

end

target 'FamilyPlannerTests' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for FamilyPlannerTests
  pod 'SwiftDate', '~> 4.4.0'

end
